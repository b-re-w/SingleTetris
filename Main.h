#define _CRT_SECURE_NO_WARNINGS // SDL 검사 오류 경고 무시

#ifndef MAIN_H
    #define MAIN_H

    #include <stdio.h>  // Standard Input/Output Header Library
    #include <string.h>  // to use memset()
    #include <time.h>  // to use time()
    #include "lib/system.h"

    #define XSIZ 12 // x축 좌표 수
    #define YSIZ 23 // y축 좌표 수
    #define SHAPENUM 6 // 도형의 개수
    #define SHAPEDIR 4 // 도형의 방위
#endif
